import React from 'react';\nimport { motion } from 'framer-motion';\n\nconst Button = ({ children, disabled = false, className = '', size = 'md', variant = 'primary', type = 'button', onClick }) => {\n  const sizes = {\n    sm: 'px-3 py-1.5 text-sm',\n    md: 'px-4 py-2 text-base',\n    lg: 'px-6 py-3 text-lg',\n  };\n\n  const variants = {\n    primary: 'bg-gradient-to-r from-emerald-500 to-emerald-600 hover:from-emerald-400 hover:to-emerald-500',\n    secondary: 'button-glass',\n    danger: 'bg-gradient-to-r from-red-500 to-red-600 hover:from-red-400',\n  };\n\n  return (\n    <motion.button\n      type={type}\n      disabled={disabled}\n      onClick={onClick}\n      className={`${sizes[size]} ${variants[variant]} font-semibold rounded-lg text-white shadow-lg disabled:opacity-50 disabled:cursor-not-allowed ${className}`}\n      whileHover={{ scale: disabled ? 1 : 1.02 }}\n      whileTap={{ scale: disabled ? 1 : 0.98 }}\n    >\n      {children}\n    </motion.button>\n  );\n};\n\nexport default Button;\n