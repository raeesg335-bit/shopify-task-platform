import React from 'react';\nimport { motion } from 'framer-motion';\nimport AnimatedCounter from './AnimatedCounter';\n\nconst StatCard = ({ title, value, icon, gradient, iconColor, isAnimated, showProgress, children }) => {\n  return (\n    <motion.div\n      className={`card-glass bg-gradient-to-br ${gradient} rounded-2xl p-6 border border-white/10`}\n      whileHover={{ y: -5, boxShadow: '0 20px 40px rgba(16, 185, 129, 0.15)' }}\n      transition={{ type: 'spring', stiffness: 300, damping: 30 }}\n    >\n      <div className=\"flex items-start justify-between mb-4\">\n        <div className={`p-3 rounded-lg bg-white/5 ${iconColor}`}>{icon}</div>\n        {showProgress && <div className=\"text-xs bg-emerald-500/20 text-emerald-300 px-2 py-1 rounded-full\">+18%</div>}\n      </div>\n      <h3 className=\"text-gray-400 text-sm font-medium mb-2\">{title}</h3>\n      <div className=\"mb-4\">\n        {isAnimated && typeof value === 'string' && value.includes('$') ? (\n          <div className=\"text-2xl font-bold text-white\">{value}</div>\n        ) : (\n          <div className=\"text-2xl font-bold text-white\">{value}</div>\n        )}\n      </div>\n      {children}\n    </motion.div>\n  );\n};\n\nexport default StatCard;\n